import Cocoa



// farenheight formula
// (value * 9)/5 + 32

let celsiusValue = 37.2

let farenheightValue = (celsiusValue * 9)/5 + 32

print("the Celsius \(celsiusValue)°C when converted to farenheight is \(farenheightValue)°F")
